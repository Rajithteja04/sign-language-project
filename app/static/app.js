const statusEl = document.getElementById("status");
const outputEl = document.getElementById("output");

statusEl.textContent = "Pipeline stub loaded.";
outputEl.textContent = "(waiting for predictions?)";
